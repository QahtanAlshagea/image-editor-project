Flask==3.1.3
Werkzeug==3.1.7
Pillow==12.1.1
numpy==2.4.4
opencv-python-headless==4.13.0.92
scipy==1.17.1

# Optional (better background removal quality). The app works fully without it
# using an OpenCV GrabCut fallback, but if you have internet access on your
# machine you can install rembg for AI-based background removal:
# rembg==2.0.59
# onnxruntime==1.19.2
